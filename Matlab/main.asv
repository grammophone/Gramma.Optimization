clear;

samples(1).Indicator = -1;
samples(1).Item = [-1, -1]';

samples(2).Indicator = -1;
samples(2).Item = [-1, 1]';

samples(3).Indicator = -1;
samples(3).Item = [-2, 0]';

samples(4).Indicator = 1;
samples(4).Item = [1, 0]';

% samples(5).Indicator = 1;
% samples(5).Item = [1, -1]';

samples(5).Indicator = 1;
samples(5).Item = [2, 0]';

optimset('', '');

lambda = svmTrain(samples, 100);

lambda
